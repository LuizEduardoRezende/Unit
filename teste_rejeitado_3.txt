/*
 * TESTE REJEITADO 3 - Erros Semânticos e de Escopo
 * Este arquivo DEVE falhar na compilação
 */

// Contexto válido
float:meters distancia_valida = 50.0;
int i = 0;

// ERRO 1: Variável não declarada
resultado_nao_declarado = 42.0;               

// ERRO 2: Uso de variável antes da declaração
valor_futuro = variavel_futura + 10;          
float variavel_futura = 5.0;

// ERRO 3: Redeclaração de variável
float:seconds tempo = 10.0;
float:meters tempo = 20.0;                      


// ERRO 4: Variável de loop não acessível fora do escopo
/*
for (int j = 0; j < 5; j = j + 1) {
    printf("Loop interno");
}
j = j + 1;                                      // j não existe fora do loop
*/

// ERRO 5: Atribuição a literal
// 100 = distancia_valida;                       // Não se pode atribuir a literal

// ERRO 6: Uso incorreto de operadores de comparação em atribuição
// distancia_valida == 100.0;                    // Comparação sem contexto

// ERRO 7: Printf com argumentos incorretos (se houvesse verificação)
// printf(100);                                  // Literal em printf (pode ser válido)
// printf();                                     // Printf sem argumentos

// ERRO 8: Operação com tipos incompatíveis (além de unidades)
// float resultado = "string" + 42;              // String + número

// ERRO 9: Condição inválida em estruturas de controle
/*
if (printf("teste")) {                          // printf não retorna booleano
    distancia_valida = 0;
}
*/

// ERRO 10: Modificação de constante (se houvesse)
// const float PI = 3.14159;
// PI = 3.0;                                     // Modificação de constante

// ERRO 11: Variável usada sem inicialização em contexto que exige valor
/*
float:meters dist_nao_inicializada;
if (dist_nao_inicializada > 0) {               // Uso sem inicialização
    printf("Valor positivo");  
}
*/

printf("Arquivo com erros semanticos");