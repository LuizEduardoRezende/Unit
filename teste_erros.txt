/*
 * NOTA IMPORTANTE: A verificação de tipos/unidades está sendo realizada durante
 * a análise sintática por simplicidade de implementação. Idealmente, essa 
 * verificação deveria ser feita na análise semântica, após a construção da
 * árvore sintática abstrata (AST).
 * 
 * CASOS DE ERRO - Este arquivo contém casos que DEVEM falhar
 * Descomente UMA linha por vez para testar cada tipo de erro
 */

// ERRO 1: Soma de unidades incompatíveis
float:meters dist = 100;
float:seconds tempo = 50;
// float:meters resultado = dist + tempo;  // ERRO: meters + seconds

// ERRO 2: Subtração de unidades incompatíveis  
// float:seconds resultado2 = tempo - dist;  // ERRO: seconds - meters

// ERRO 3: Atribuição de unidades incompatíveis
// dist = tempo;  // ERRO: meters = seconds

// ERRO 4: Comparação de unidades diferentes
/*
if (dist < tempo) { 
    printf("Erro");
}
*/

// ERRO 5: Multiplicação sem regra definida
float:seconds t1 = 10;
float:seconds t2 = 5;
// float:meters resultado3 = t1 * t2;  // ERRO: seconds * seconds (não há regra)

// ERRO 6: Divisão sem regra definida
// float:meters resultado4 = tempo / dist;  // ERRO: seconds / meters (não definido)

// ERRO 7: Variável não declarada
// resultado_inexistente = 42;  // ERRO: variável não declarada

// ERRO 8: Atribuição para declaração com unidade incompatível
float:meters distancia_errada;
// distancia_errada = tempo;  // ERRO: meters = seconds
