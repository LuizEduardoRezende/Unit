/*
 * TESTE REJEITADO 2 - Erros Sintáticos Individuais
 * Este arquivo DEVE falhar na compilação
 * 
 * INSTRUÇÕES:
 * - SEM recuperação de erros implementada - parser para no primeiro erro
 * - Apenas UM erro está ativo por vez
 * - Para testar outros erros, comente o erro ativo e descomente outro
 * - Cada teste mostra apenas o primeiro erro encontrado
 */

// Declarações válidas para contexto
float:meters dist = 100.0;
int contador = 0;

// ERRO 1: Ponto e vírgula ausente (ATIVO)
dist = 200

/*
=== OUTROS ERROS PARA TESTAR (UM POR VEZ) ===

// ERRO 2: Sintaxe incorreta em declaração
float:meters;

// ERRO 3: Parênteses não balanceados
if (dist > 50 {
    printf("Teste");
}

// ERRO 4: Operação incompleta
dist = 50 + ;

// ERRO 5: Chaves não balanceadas
for (int i = 0; i < 5; i = i + 1) {
    printf("Loop");
// Falta fechar chave

// ERRO 6: Estrutura FOR malformada
for (int i = 0 i < 5; i = i + 1) {
    printf("Loop");
}

// ERRO 7: Condição IF sem parênteses
if dist > 100 {
    printf("Grande");
}

// ERRO 8: ELSE sem IF correspondente
} else {
    printf("Pequeno");
}

// ERRO 9: Expressão sem fechamento de parênteses
dist = (100 + 50;

// ERRO 10: Vírgula sem contexto
float valor, = 100.0;

