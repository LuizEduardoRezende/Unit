/*
 * TESTE REJEITADO 1 - Erros de Incompatibilidade de Unidades
 * Este arquivo DEVE falhar na compilação
 */

// Declarações válidas
float:meters distancia = 100.0;
float:seconds tempo = 20.0;
float:m_per_s velocidade = 10.0;
float:square_meter area = 50.0;

// ERRO 1: Soma de unidades incompatíveis
float:meters erro1 = distancia + tempo;           // meters + seconds (INVÁLIDO)

// ERRO 2: Subtração de unidades incompatíveis  
float:seconds erro2 = tempo - distancia;       // seconds - meters (INVÁLIDO)

// ERRO 3: Atribuição de unidades incompatíveis
distancia = tempo;                             // meters = seconds (INVÁLIDO)

// ERRO 4: Multiplicação sem regra definida
float:meters erro4 = tempo * distancia;        // seconds * meters (SEM REGRA)

// ERRO 5: Divisão sem regra definida
float:meters erro5 = tempo / velocidade;       // seconds / m_per_s (SEM REGRA)

// ERRO 6: Comparação de unidades diferentes em condicionais

if (distancia < tempo) {                         // meters < seconds (INVÁLIDO)
    printf("Erro logico");
}


// ERRO 7: Atribuição em declaração com unidade incompatível
float:meters erro7 = tempo;                    // meters = seconds (INVÁLIDO)

// ERRO 8: Operação inválida em loop
for (int i = 0; i < 5; i = i + 1) {
    distancia = distancia + tempo;               // meters + seconds (INVÁLIDO)
}

